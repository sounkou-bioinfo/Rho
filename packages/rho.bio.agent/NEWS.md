# rho.bio.agent 0.0.1.9001

- Advances with the synchronized monorepo package release so downstream
  installations resolve matching internal dependencies.

# rho.bio.agent 0.0.1.9000

- Declares `rho.bio` as the dependency used by its executable specifications.

- Adds the experimental downstream bridge from declared bioinformatics
  resources and operations to Rho extension and tool protocols.
