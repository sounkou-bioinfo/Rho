# rho.compute 0.0.1.9001

- Advances with the synchronized monorepo package release so dependent Rho
  packages resolve a matching compute contract.

# rho.compute 0.0.1.9000

- Adds the experimental mirai compute backend for asynchronous R expressions
  and function calls.
- Propagates task cancellation to mirai and resolves cancellation as a typed
  value.
