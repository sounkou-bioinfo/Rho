#!/usr/bin/env sh
set -eu

repository_root="$(git rev-parse --show-toplevel)"
cd "$repository_root"

printf '%s\n' 'pre-push: scanning Git history and working tree for secrets'
exec Rscript scripts/check-secrets.R
